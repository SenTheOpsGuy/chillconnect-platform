# Patterns to detect potential secrets and credentials
# This file helps prevent accidental commits of sensitive information

# API Keys
[A-Za-z0-9]{32,}
sk_[a-z]+_[A-Za-z0-9]+
pk_[a-z]+_[A-Za-z0-9]+

# PayPal patterns
A[A-Za-z0-9_-]{20,}
E[A-Za-z0-9_-]{20,}

# Database URLs
postgresql://
mysql://
mongodb://
redis://

# JWT secrets
eyJ[A-Za-z0-9_-]*\.[A-Za-z0-9_-]*\.[A-Za-z0-9_-]*

# Common secret patterns
password\s*[:=]\s*['"][^'"]*['"]
secret\s*[:=]\s*['"][^'"]*['"]
token\s*[:=]\s*['"][^'"]*['"]
key\s*[:=]\s*['"][^'"]*['"]

# Email patterns with passwords
@.*password
password.*@

# Credit card patterns (just in case)
[0-9]{4}[\s-]?[0-9]{4}[\s-]?[0-9]{4}[\s-]?[0-9]{4}

# Phone numbers (if considered sensitive)
\+91[\s-]?[0-9]{10}